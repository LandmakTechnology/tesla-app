- hosts: eks  
  tasks: 
    - name: copy deployment manifest files   
      copy: src=princebabs.yml dest=/home/eks/princebabs.yml    
    - name: deploy the web application 
      shell: kubectl apply -f /home/eks/princebabs.yml 
